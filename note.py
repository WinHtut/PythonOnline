#Random 
#Secure by crytography
#Start writing a game using secure rand
#Random 
#random.random()    return floating point 0.0 and 1.0
#                       Int (return)
#                           /\
#                          /  \
#                      randint randrange
#                        /      \
# a <= Number <=b randint(0,9)  randrange(0,10,2) (start , stop , step)
#                               The step is optional default is 1
############
#random.sample(population,k ) example list, set or tuple
# count of the totoal elements depends on the size of k.

###########
#random.seed()
#seed method is used to PRNGs (pseudorandom number generator)
#seed value is not present (system current time)

###########
#random.shuffle(list or set or etc)

###########
#random.uniforn(start,end) return floating point number 

############
#random.triangular(low,high,mode) for floating number
#lower <= N <= Upper 
# 0 is default for lower
# 1 is default for upper










